---
import { site } from '../data/site';
import { images } from '../data/images';
import Sparkles from './Sparkles.astro';

interface Props {
  title?: string;
  subtitle?: string;
}

const {
  title = 'Get Started With Professional Cleaning',
  subtitle = 'Reach out now for professional cleaning solutions with unmatched detail and reliability. We’re ready to help you.',
} = Astro.props;
---

<section class="relative overflow-hidden">
  <img src={images.areas.skyline} alt="" aria-hidden="true" class="absolute inset-0 h-full w-full object-cover" loading="lazy" />
  <div class="absolute inset-0 bg-ink/80" aria-hidden="true"></div>
  <Sparkles
    items={[
      { top: '20%', right: '14%', size: 6, delay: 0, tone: 'white' },
      { top: '52%', left: '12%', size: 5, delay: 2.4, tone: 'white' },
      { bottom: '18%', right: '28%', size: 7, delay: 1.2, tone: 'white' },
    ]}
  />
  <div class="relative mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
    <div class="reveal mx-auto flex max-w-3xl flex-col items-center gap-7 text-center">
      <h2 class="font-display text-3xl font-semibold tracking-tight text-white sm:text-4xl">{title}</h2>
      <p class="max-w-xl text-base leading-relaxed text-white/65">{subtitle}</p>
      <div class="mt-2 flex flex-col gap-3 sm:flex-row">
        <a href="/contact/#quote" class="btn-primary px-8 py-3.5 text-base">Get a Free Quote</a>
        <a href={site.phoneHref} class="btn-outline-light px-8 py-3.5 text-base">
          <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" aria-hidden="true">
            <path d="M3.5 4.5c0-1.1.9-2 2-2h2l1.5 4-2 1.5a11 11 0 005 5l1.5-2 4 1.5v2c0 1.1-.9 2-2 2A12.5 12.5 0 013.5 4.5z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round" />
          </svg>
          Call {site.phone}
        </a>
      </div>
      <p class="text-xs text-white/45">
        {site.hours.weekdays} · {site.hours.weekends} · {site.address}
      </p>
    </div>
  </div>
</section>
